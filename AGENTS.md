# the gitfather

source: SPEC.md and the person's later instructions.

build the entire scaffold first. then complete each layer across state, commands,
hooks, packaging and documentation. during construction use compile/syntax smoke
only. once joined, run integrated proof, repair observed failures narrowly, and
release the tested source. no per-function finishing loops.

root owns design and acceptance. workers implement bounded files from SPEC.md.
do not change CompactVeteran, RecentlyDivorced, PartOfThis, model settings or
existing hook trust. no extra model calls, transcript crawlers or background jobs.

readme: lowercase prose, correctly cased brands, a small ruled schematic,
install, usage, controls, state, uninstall, build. no marketing headings.
